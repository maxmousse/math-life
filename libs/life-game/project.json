{
  "name": "life-game",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "./libs/life-game/src",
  "targets": {
    "build": {
      "outputs": ["{options.output}"],
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx wasm-pack build libs/life-game --out-dir ../../dist/wasm/life-game --target bundler --dev --scope ml",
          "npm i dist/wasm/life-game"
        ],
        "parallel": false,
        "output": "dist/wasm/life-game"
      }
    },
    "test": {
      "executor": "@monodon/rust:test",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/life-game"
      },
      "configurations": {
        "production": {
          "release": true
        }
      }
    },
    "lint": {
      "executor": "@monodon/rust:lint",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/life-game"
      }
    }
  },
  "tags": []
}
